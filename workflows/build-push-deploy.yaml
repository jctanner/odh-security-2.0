name: "Build, Push and Deploy OpenDataHub"
description: "Complete workflow to build operator image, push to registry, and deploy to cluster"

# Include modular workflows in order
includes:
  - "build"
  - "push"  
  - "deploy"

# No additional steps needed - everything is handled by included workflows
steps: [] 