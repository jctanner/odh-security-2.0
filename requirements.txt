# OpenDataHub Gateway API Migration Tool Dependencies
# Core dependencies for the tool to function

# Ansible for workflow execution
ansible-core>=2.15.0,<3.0.0

# YAML processing (usually included with Python, but explicit for clarity)
PyYAML>=6.0

# HTTP requests for GitHub API interactions
requests>=2.28.0

# Additional useful modules that complement ansible-core
# (these provide additional Ansible modules if needed)
ansible-posix>=1.5.0  # POSIX system modules 